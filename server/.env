PORT=5000
NODE_ENV=development
JWT_SECRET=gatekeeper_jwt_super_secret_production_key_2026
# Optional: MongoDB connection string (falls back to embedded MongoMemoryServer if omitted)
# MONGO_URI=mongodb://localhost:27017/gatekeeper
# Optional: Production Nodemailer SMTP credentials (falls back to Ethereal Email test account if omitted)
# EMAIL_USER=organizer@gmail.com
# EMAIL_APP_PASSWORD=abcd1234efgh5678
# EMAIL_FROM="GateKeeper Events" <tickets@gatekeeper.io>
CLIENT_URL=http://localhost:5173
